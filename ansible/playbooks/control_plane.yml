- name: Verify connection to control plane nodes
  hosts: control_plane
  gather_facts: false
  tasks:
  - name: Ping the control plane nodes
    ping:

  - name: Check uptime of the control plane nodes
    command: uptime
    register: uptime_result

  - name: Display uptime
    debug:
      var: uptime_result.stdout
