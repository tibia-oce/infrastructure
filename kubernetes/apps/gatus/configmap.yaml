# gatus/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: gatus
  namespace: apps
data:
  config.yaml: |
    metrics: true
    endpoints:
      - name: GitHub
        url: https://api.github.com/healthz
        interval: 5m
        conditions:
          - "[STATUS] == 200"
